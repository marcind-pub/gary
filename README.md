# Simple Static Page

A minimal, framework-free static page for GitHub Pages.

## Publish

Deploy the repository root to GitHub Pages. The site entry point is `index.html`.

## Customize

Edit `index.html` to change the page title, copy, or visual style.